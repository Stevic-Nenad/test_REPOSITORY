name: Dispatch Responder
on:
  repository_dispatch:
    types: [test-event]

jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - name: Show this workflow was triggered
        run: |
          echo "🤖 Dispatch responder triggered!"
          echo "Source: ${{ github.event.client_payload.source }}"
          echo "Actor: ${{ github.actor }}"